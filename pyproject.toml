[project]
name = "social-network"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.2",
    "pillow>=12.1.1",
    "pytest>=9.0.2",
    "pytest-django>=4.12.0",
]
